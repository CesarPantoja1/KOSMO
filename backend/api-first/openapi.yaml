openapi: 3.1.0
info:
  title: KOSMO API
  version: "0.1.0"
  description: >-
    API de KOSMO - plataforma low-code de spec-driven development (SDD) asistida por IA.
    Ciclo completo: Descubrimiento -> Características -> Requisitos -> Modelo -> Código.

    Convenciones:
    - Autenticación PKCE (RFC 7636) con JWT RS256 (bearerAuth).
    - Errores RFC 7807 (application/problem+json) con type URN, trace_id y violations, o OAuthErrorResponse (RFC 6749) / HttpErrorResponse.
    - Fechas ISO-8601 UTC con sufijo Z.
    - Las fases siguen el orden Descubrimiento -> Características -> Requisitos -> Modelo -> Código.
      La consistencia se evalúa exclusivamente hacia la derecha (downstream).
  contact:
    name: Equipo KOSMO
    email: dev@kosmo.app
    url: https://github.com/CesarPantoja1/KOSMO
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: http://localhost:8000
    description: Local — desarrollo en máquina del programador
  - url: https://api-dev.kosmo.app
    description: Desarrollo — entorno de integración continua
  - url: https://api.kosmo.app
    description: Producción — tráfico real de usuarios

tags:
  - name: health
    description: Verificación de disponibilidad del servidor
  - name: auth
    description: Flujo de autenticación PKCE + OAuth 2.0 (RFC 6749/7636) con JWT RS256
  - name: projects
    description: Gestión del ciclo de vida de proyectos
  - name: discovery
    description: Fase de Descubrimiento — documento de visión de producto
  - name: features
    description: Fase de Características funcionales del producto software
  - name: requirements
    description: Fase de Requisitos (EARS) por característica
  - name: modelo
    description: Fase de Modelo — diagramas de actividad PlantUML
  - name: consistency
    description: Evaluación y resolución de consistencia downstream entre fases (gate)
  - name: chat-sessions
    description: Gestión de sesiones e hilos de chat interactivo por fase
  - name: documents
    description: Modificación directa y versionado/reversión de documentos
  - name: schemas
    description: Introspección de esquemas JSON para formularios dinámicos
  - name: knowledge
    description: Consolidación y gestión de patrones de conocimiento cross-project
  - name: Traceability
    description: Navegación y guardas de trazabilidad entre entidades y fases
  - name: MCP Tools
    description: Herramientas de contexto para agentes de código bajo protocolo MCP
  - name: Implementations
    description: Generación y consulta de código fuente / implementaciones vía OpenCode
  - name: ai-config
    description: Configuración y verificación del proveedor de Inteligencia Artificial (BYOK)
  - name: integrations
    description: Gestión de vinculación OAuth con plataformas externas (GitHub, Railway)
  - name: github
    description: Sincronización y subida de código fuente a repositorios de GitHub
  - name: deploy
    description: Orquestación y monitoreo del despliegue en la nube (Railway)

security:
  - bearerAuth: []

paths:
  /health:
    get:
      tags: [health]
      summary: Health check
      description: >-
        Verificacion de disponibilidad del servidor. Devuelve status ok si el proceso esta activo.
      operationId: health_check
      security: []
      responses:
        "200":
          description: Servidor activo
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                required: [status]

  # ══════════════════════════════ AUTH ══════════════════════════════

  /api/v1/auth/register:
    post:
      tags: [auth]
      summary: Registrar nuevo usuario
      description: >-
        Crea una nueva cuenta de usuario en KOSMO. La contrasena se procesa con Argon2id (OWASP 2025).
        Si el email ya existe se devuelve 409 Conflict. Limite de velocidad: 3 peticiones / IP / ventana.
      operationId: auth_register
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Cuenta creada exitosamente. Devuelve los datos publicos del usuario.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserPublic"
        "409":
          description: Email ya registrado en el sistema
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"
              example:
                error: email_already_registered
                error_description: Email ya registrado
        "429":
          description: Rate limit excedido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"

  /api/v1/auth/authorize:
    post:
      tags: [auth]
      summary: Iniciar autenticacion PKCE — obtener codigo de autorizacion
      description: >-
        Valida las credenciales del usuario e inicia el flujo PKCE. Emite un authorization_code
        de un solo uso (TTL: 5 min) vinculado al code_challenge enviado. Tras 5 intentos fallidos
        consecutivos se responde con 429 y header Retry-After. Limite: 10 peticiones / IP / ventana.
      operationId: auth_authorize
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthorizeRequest"
      responses:
        "201":
          description: Codigo de autorizacion emitido. Usar en POST /token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthorizationCodeResponse"
        "401":
          description: Credenciales invalidas (email no encontrado o contrasena incorrecta)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"
        "429":
          description: Cuenta bloqueada por intentos fallidos excesivos
          headers:
            Retry-After:
              schema:
                type: string
              description: Segundos restantes de bloqueo
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"

  /api/v1/auth/token:
    post:
      tags: [auth]
      summary: Intercambiar codigo de autorizacion por par de tokens JWT
      description: >-
        Completa el flujo PKCE: valida el code_verifier contra el code_challenge almacenado,
        consume el authorization_code y emite un par de tokens JWT firmados con RS256 (access TTL 15m, refresh TTL 7d).
        Limite: 5 peticiones / IP / ventana.
      operationId: auth_token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TokenExchangeRequest"
      responses:
        "200":
          description: Par de tokens emitido exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenPairResponse"
        "400":
          description: Codigo invalido, expirado, consumido o code_verifier no coincide
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"
        "429":
          description: Rate limit excedido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"

  /api/v1/auth/refresh:
    post:
      tags: [auth]
      summary: Renovar par de tokens usando refresh token
      description: >-
        Implementa Token Rotation: consume el refresh token presentado, lo revoca en Redis y emite un par nuevo.
        Si se detecta reutilizacion, toda la familia de tokens queda revocada. Limite: 30 peticiones / IP / ventana.
      operationId: auth_refresh
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TokenRefreshRequest"
      responses:
        "200":
          description: Par de tokens renovado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenPairResponse"
        "401":
          description: Refresh token invalido, expirado, revocado o reutilizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"
        "429":
          description: Rate limit excedido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"

  /api/v1/auth/me:
    get:
      tags: [auth]
      summary: Obtener identidad del usuario autenticado
      description: >-
        Decodifica y verifica el access_token del header Authorization: Bearer.
        Devuelve el subject (ID de usuario) y los scopes de la sesion activa.
      operationId: auth_me
      responses:
        "200":
          description: Identidad verificada del token presentado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PrincipalView"
        "401":
          description: Token ausente, mal formado, con firma invalida, expirado o revocado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"

  /api/v1/auth/logout:
    post:
      tags: [auth]
      summary: Cerrar sesion y revocar tokens activos
      description: >-
        Revoca el access_token del header Authorization: Bearer y opcionalmente el refresh_token del body.
        Ambos quedan invalidados en Redis. Limite: 20 peticiones / IP / ventana.
      operationId: auth_logout
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogoutRequest"
      responses:
        "204":
          description: Sesion cerrada exitosamente. Sin cuerpo de respuesta.
        "401":
          description: Token ausente, mal formado, expirado o ya revocado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"
        "429":
          description: Rate limit excedido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthErrorResponse"

  # ══════════════════════════════ PROJECTS ══════════════════════════════

  /api/v1/projects:
    post:
      tags: [projects]
      summary: Crear nuevo proyecto
      description: >-
        Crea un nuevo proyecto en KOSMO. El nombre se transforma automaticamente en un slug unico.
        Requiere autenticacion mediante Bearer token.
      operationId: project_create
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRequest"
      responses:
        "201":
          description: Proyecto creado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      tags: [projects]
      summary: Listar proyectos del usuario
      description: Devuelve todos los proyectos del usuario autenticado. Requiere Bearer token.
      operationId: project_list
      responses:
        "200":
          description: Lista de proyectos del usuario
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/projects/{project_id}:
    get:
      tags: [projects]
      summary: Obtener proyecto por ID
      description: Devuelve los detalles de un proyecto especifico. Requiere Bearer token.
      operationId: project_get
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Detalles del proyecto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [projects]
      summary: Eliminar proyecto
      description: >-
        Elimina el proyecto y todos sus artefactos en cascada: descubrimiento, caracteristicas,
        requisitos, modelos, chat y evaluaciones de consistencia. Requiere Bearer token.
      operationId: project_delete
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Proyecto eliminado exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: deleted
                  project_id:
                    type: string
                required: [status, project_id]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/preview:
    get:
      tags: [projects]
      summary: URL de la vista previa del proyecto (obsoleto)
      deprecated: true
      description: >-
        [DEPRECATED] Devuelve la URL de la vista previa del proyecto si tiene una implementacion exitosa
        con puerto asignado en el workspace. A partir del Sprint 6, este endpoint queda en desuso y se
        reemplaza por el flujo de publicacion en la nube (/api/v1/projects/{project_id}/deploy/railway).
      operationId: project_preview_get
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: URL publica de vista previa
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectPreviewResponse"
        "404":
          description: El proyecto no tiene una vista previa activa
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  # ══════════════════════════════ DISCOVERY ══════════════════════════════

  /api/v1/projects/{project_id}/discovery:
    post:
      tags: [discovery]
      summary: Generar documento de descubrimiento con IA
      description: Genera un documento de vision de producto estructurado en 8 secciones mediante IA.
      operationId: discovery_generate
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Documento generado exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  project_id:
                    type: string
                  content:
                    type: string
                required: [project_id, content]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      tags: [discovery]
      summary: Obtener documento de descubrimiento
      description: Devuelve el documento de descubrimiento almacenado de un proyecto.
      operationId: discovery_get
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Documento de descubrimiento del proyecto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [discovery]
      summary: Guardar documento de descubrimiento
      description: Persiste manualmente un documento de descubrimiento sin invocar al agente IA.
      operationId: discovery_save
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveDiscoveryRequest"
      responses:
        "200":
          description: Documento guardado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/projects/{project_id}/discovery/refine:
    post:
      tags: [discovery]
      summary: Refinar documento de descubrimiento con IA
      description: >-
        Refina el documento de descubrimiento aplicando las instrucciones del usuario mediante IA.
        Las instrucciones no pueden superar los 500 caracteres.
      operationId: discovery_refine
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefineDiscoveryRequest"
      responses:
        "200":
          description: Documento refinado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiscoveryResponse"
        "400":
          description: Las instrucciones exceden los 500 caracteres o son invalidas
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          description: Error al invocar el servicio de IA
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  /api/v1/projects/{project_id}/discovery/chat:
    post:
      tags: [discovery]
      summary: Enviar mensaje al chat de Descubrimiento (aplicacion instantanea)
      description: >-
        Procesa el mensaje y aplica el cambio inmediatamente sobre el documento cuando corresponde,
        verificando consistencia downstream. Si corresponde a otra fase, devuelve redireccion.
      operationId: discovery_chat_send
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendChatRequest"
      responses:
        "200":
          description: Mensaje procesado, modificacion aplicada o redireccion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatResponse"
        "400":
          description: Error de validacion o tamano de mensaje
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflicto de transicion de fase
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "502":
          description: Error al invocar el LLM
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
    get:
      tags: [discovery]
      summary: Obtener historial de chat de Descubrimiento
      description: Devuelve los mensajes del chat interactivo para la fase de Descubrimiento.
      operationId: discovery_chat_history
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/CursorBefore"
        - $ref: "#/components/parameters/SessionIdQuery"
      responses:
        "200":
          description: Historial obtenido exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatHistoryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/discovery/chat/stream:
    post:
      tags: [discovery]
      summary: Enviar mensaje al chat de Descubrimiento con streaming SSE
      description: Genera respuesta en tiempo real (SSE). Emite eventos start, chunk y message.
      operationId: discovery_chat_stream
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendChatRequest"
      responses:
        "200":
          description: Stream de eventos SSE
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/ChatStreamEvent"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  # ══════════════════════════════ FEATURES ══════════════════════════════

  /api/v1/projects/{project_id}/features:
    post:
      tags: [features]
      summary: Generar caracteristicas del producto con IA
      description: Genera caracteristicas de alto nivel evaluando el documento de descubrimiento del proyecto.
      operationId: features_generate
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Caracteristicas generadas exitosamente
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FeatureResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      tags: [features]
      summary: Listar caracteristicas del proyecto
      description: Devuelve todas las caracteristicas asociadas a un proyecto.
      operationId: features_list
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Lista de caracteristicas del proyecto
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FeatureResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/projects/{project_id}/features/suggest:
    post:
      tags: [features]
      summary: Sugerir nuevas caracteristicas con IA
      description: Sugiere caracteristicas adicionales basadas en el documento de descubrimiento evitando duplicados.
      operationId: features_suggest
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Sugerencias de caracteristicas generadas
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FeatureSuggestionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/projects/{project_id}/features/manual:
    post:
      tags: [features]
      summary: Crear caracteristica manualmente
      description: >-
        Crea una nueva caracteristica. Si no se indica origin, la IA lo deriva del descubrimiento
        y verifica coherencia. Ante inconsistencia devuelve is_saved=false con la razon.
      operationId: features_create_manual
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCharacteristicRequest"
      responses:
        "200":
          description: Caracteristica creada exitosamente o rechazada por inconsistencia
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateCharacteristicResult"
        "400":
          description: Datos invalidos (titulo vacio o excesivo, descripcion muy larga)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/v1/projects/{project_id}/features/{feature_id}/manual:
    put:
      tags: [features]
      summary: Editar caracteristica manualmente
      description: >-
        Edita una caracteristica existente. Si los cambios contradicen flagrantemente el descubrimiento,
        el guardado es rechazado devolviendo 409 Conflict.
      operationId: features_edit_manual
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditFeatureManualRequest"
      responses:
        "200":
          description: Caracteristica editada exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Contradiccion detectada con el documento fuente
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  /api/v1/projects/{project_id}/features/save:
    post:
      tags: [features]
      summary: Guardar caracteristicas seleccionadas
      description: Guarda las caracteristicas seleccionadas por el usuario desde las sugerencias de la IA.
      operationId: features_save_selected
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveSelectedFeaturesRequest"
      responses:
        "201":
          description: Caracteristicas guardadas exitosamente
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FeatureResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/projects/{project_id}/features/{feature_id}:
    delete:
      tags: [features]
      summary: Eliminar caracteristica
      description: Elimina una caracteristica y todos sus artefactos asociados (requisitos, diagramas).
      operationId: features_delete
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/FeatureId"
      responses:
        "200":
          description: Caracteristica eliminada exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: deleted
                  feature_id:
                    type: string
                required: [status, feature_id]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/features/{feature_id}/consistency/check:
    post:
      tags: [features]
      summary: Verificar consistencia antes de guardado manual
      description: >-
        Verifica que el contenido editado no contradiga el descubrimiento invocando al agente IA.
      operationId: features_check_consistency
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckConsistencyRequestView"
      responses:
        "200":
          description: Resultado de la verificacion de consistencia
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InconsistencyResultView"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ FEATURE CHAT ══════════════════════════════

  /api/v1/features/{feature_id}/chat:
    post:
      tags: [features]
      summary: Enviar mensaje al chat de Caracteristicas (aplicacion instantanea)
      description: >-
        Procesa el mensaje y aplica el cambio inmediatamente sobre la caracteristica (titulo, descripcion, origen),
        verificando consistencia downstream en Requisitos y Modelo. Si pertenece a otra fase, devuelve redireccion.
      operationId: feature_chat_send
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendChatRequest"
      responses:
        "200":
          description: Mensaje procesado, modificacion aplicada o redireccion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflicto de fase
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/features/{feature_id}/chat/history:
    get:
      tags: [features]
      summary: Obtener historial del chat de la caracteristica
      description: Devuelve todos los mensajes del chat interactivo para una caracteristica especifica.
      operationId: feature_chat_history
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/CursorBefore"
        - $ref: "#/components/parameters/SessionIdQuery"
      responses:
        "200":
          description: Historial obtenido exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatHistoryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/features/{feature_id}/chat/stream:
    post:
      tags: [features]
      summary: Enviar mensaje al chat de Caracteristicas con streaming SSE
      description: Genera respuesta en tiempo real (SSE). Emite eventos start, chunk y message.
      operationId: feature_chat_stream
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendChatRequest"
      responses:
        "200":
          description: Stream de eventos SSE
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/ChatStreamEvent"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ REQUIREMENTS ══════════════════════════════

  /api/v1/features/{feature_id}/requirements/generate:
    post:
      tags: [requirements]
      summary: Generar requisitos EARS con IA
      description: Genera requisitos EARS para la caracteristica indicada.
      operationId: requirements_generate
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateRequirementsRequest"
      responses:
        "200":
          description: Requisitos generados exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateRequirementsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/features/{feature_id}/requirements:
    get:
      tags: [requirements]
      summary: Obtener requisitos de una caracteristica
      description: Recupera los requisitos en formato Markdown asociados a la caracteristica.
      operationId: requirements_get
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/ProjectIdQuery"
      responses:
        "200":
          description: Documento de requisitos en Markdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequirementsDocumentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags: [requirements]
      summary: Guardar/actualizar requisitos
      description: Actualiza el documento Markdown de requisitos de la caracteristica especificada.
      operationId: requirements_save
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveRequirementsRequest"
      responses:
        "200":
          description: Requisitos guardados exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  feature_id:
                    type: string
                  message:
                    type: string
                    example: ok
                required: [feature_id, message]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [requirements]
      summary: Eliminar requisitos
      description: Elimina el documento de requisitos EARS de la caracteristica especificada.
      operationId: requirements_delete
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/ProjectIdQuery"
      responses:
        "200":
          description: Requisitos eliminados exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: deleted
                  feature_id:
                    type: string
                required: [status, feature_id]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/features/{feature_id}/requirements/refine:
    post:
      tags: [requirements]
      summary: Refinar requisitos EARS con IA
      description: >-
        Refina los requisitos de una caracteristica aplicando las instrucciones del usuario.
        Requiere que la caracteristica ya tenga requisitos generados.
      operationId: requirements_refine
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefineRequirementsRequest"
      responses:
        "200":
          description: Requisitos refinados exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefineRequirementsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/features/{feature_id}/requirements/regenerate:
    post:
      tags: [requirements]
      summary: Regenerar requisitos EARS con IA
      description: >-
        Regenera los requisitos de una caracteristica a partir del estado actual de la caracteristica padre,
        manteniendo la estructura EARS y criterios de aceptacion Dado-Cuando-Entonces.
      operationId: requirements_regenerate
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/ProjectIdQuery"
      responses:
        "200":
          description: Requisitos regenerados exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegenerateRequirementsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  # ══════════════════════════════ REQUIREMENT CHAT ══════════════════════════════

  /api/v1/features/{feature_id}/requirements/chat:
    post:
      tags: [requirements]
      summary: Enviar mensaje al chat de Requisitos (aplicacion instantanea)
      description: >-
        Procesa el mensaje y aplica el cambio inmediatamente sobre el documento de requisitos,
        verificando la consistencia downstream en el Modelo.
      operationId: requirement_chat_send
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendChatRequest"
      responses:
        "200":
          description: Mensaje procesado, modificacion aplicada o redireccion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflicto de fase
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/features/{feature_id}/requirements/chat/history:
    get:
      tags: [requirements]
      summary: Obtener historial del chat de un requisito
      description: Devuelve todos los mensajes del chat interactivo para un requisito especifico.
      operationId: requirement_chat_history
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/CursorBefore"
        - $ref: "#/components/parameters/SessionIdQuery"
      responses:
        "200":
          description: Historial obtenido exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatHistoryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/features/{feature_id}/requirements/chat/stream:
    post:
      tags: [requirements]
      summary: Enviar mensaje al chat de Requisitos con streaming SSE
      description: Genera respuesta en tiempo real (SSE). Emite eventos start, chunk y message.
      operationId: requirement_chat_stream
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendChatRequest"
      responses:
        "200":
          description: Stream de eventos SSE
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/ChatStreamEvent"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ MODELO ══════════════════════════════

  /api/v1/features/{feature_id}/diagram/generate:
    post:
      tags: [modelo]
      summary: Generar diagrama de actividad PlantUML
      description: Genera un diagrama PlantUML a partir de los requisitos EARS de la caracteristica indicada.
      operationId: diagram_generate
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateDiagramRequest"
      responses:
        "200":
          description: Diagrama generado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagramResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/features/{feature_id}/diagram/propagate:
    post:
      tags: [modelo]
      summary: Propagar cambios al modelo (regenerar)
      description: Regenera el diagrama de actividad PlantUML cuando cambios upstream lo dejaron desactualizado.
      operationId: diagram_propagate
      parameters:
        - $ref: "#/components/parameters/FeatureId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateDiagramRequest"
      responses:
        "200":
          description: Diagrama regenerado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagramResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/features/{feature_id}/diagram:
    get:
      tags: [modelo]
      summary: Obtener diagrama de actividad
      description: Recupera el diagrama de actividad PlantUML existente de la caracteristica.
      operationId: diagram_get
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/ProjectIdQuery"
      responses:
        "200":
          description: Diagrama de actividad existente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiagramResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [modelo]
      summary: Eliminar diagrama de actividad
      description: Elimina el diagrama de actividad PlantUML de la caracteristica.
      operationId: diagram_delete
      parameters:
        - $ref: "#/components/parameters/FeatureId"
        - $ref: "#/components/parameters/ProjectIdQuery"
      responses:
        "200":
          description: Diagrama eliminado exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: deleted
                  feature_id:
                    type: string
                required: [status, feature_id]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ CONSISTENCY ══════════════════════════════

  /api/v1/projects/{project_id}/consistency/status:
    get:
      tags: [consistency]
      summary: Estado de consistencia pendiente por fase
      description: Badges por fase calculados sobre las evaluaciones no resueltas del proyecto.
      operationId: consistency_status
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Conteos de estado de consistencia por fase
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsistencyStatusView"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/consistency/review:
    get:
      tags: [consistency]
      summary: Vista de revision de consistencia por fase (gate)
      description: Cards frescas por artefacto destino. Las sugerencias obsoletas se descartan automaticamente.
      operationId: consistency_review
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: target_phase
          in: query
          required: true
          schema:
            type: string
            enum: [features, requirements, model]
          description: Fase destino a revisar
      responses:
        "200":
          description: Cards agrupadas por artefacto destino
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsistencyReviewView"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/consistency/evaluations/{evaluation_id}/apply:
    post:
      tags: [consistency]
      summary: Aplicar una sugerencia de consistencia
      description: Aplica con guardrail de frescura; si la entrada cambio devuelve 409 Conflict.
      operationId: consistency_apply_evaluation
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: evaluation_id
          in: path
          required: true
          schema:
            type: string
          description: ID de la evaluacion (cev_ + ULID)
      responses:
        "200":
          description: Sugerencia aplicada exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyEvaluationResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: La sugerencia ya no aplica al estado actual
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  /api/v1/projects/{project_id}/consistency/evaluations/{evaluation_id}/discard:
    post:
      tags: [consistency]
      summary: Descartar una sugerencia de consistencia
      description: Descartada para este snapshot; si la fuente vuelve a cambiar, aparece una sugerencia nueva.
      operationId: consistency_discard_evaluation
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: evaluation_id
          in: path
          required: true
          schema:
            type: string
          description: ID de la evaluacion (cev_ + ULID)
      responses:
        "200":
          description: Sugerencia descartada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyEvaluationResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflicto o sugerencia ya procesada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  /api/v1/projects/{project_id}/consistency/review/bulk:
    post:
      tags: [consistency]
      summary: Resolver en lote las sugerencias de una fase
      description: Aplica o descarta todas las sugerencias frescas de la fase destino en un solo clic.
      operationId: consistency_bulk_resolve
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkResolveRequestView"
      responses:
        "200":
          description: Resultado de la resolucion en lote
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkResolveResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Action no valida o fase destino desconocida
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  /api/v1/projects/{project_id}/consistency/activity:
    get:
      tags: [consistency]
      summary: Feed de actividad de consistencia
      description: Historial de sugerencias aplicadas o descartadas con su origen.
      operationId: consistency_activity
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
          description: Numero maximo de entradas
      responses:
        "200":
          description: Feed de actividad de consistencia
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsistencyActivityView"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/consistency/evaluate:
    post:
      tags: [consistency]
      summary: Evaluar consistencia entre fases
      description: >-
        Evalua el impacto de cambios sobre las fases a la derecha (downstream)
        segun el orden Descubrimiento -> Caracteristicas -> Requisitos -> Modelo.
        Si se intenta evaluar hacia la izquierda (upstream) devuelve 422.
      operationId: consistency_evaluate
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvaluateConsistencyRequestView"
      responses:
        "200":
          description: Reporte de impacto de consistencia
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConsistencyReportView"
        "400":
          description: Fase de origen o destino desconocida
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Direccion de evaluacion no permitida (upstream)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  /api/v1/projects/{project_id}/consistency/evaluate/stream:
    post:
      tags: [consistency]
      summary: Evaluar consistencia entre fases (SSE streaming)
      description: Evalua el impacto de cambios en cascada con emision de progreso en tiempo real mediante SSE.
      operationId: consistency_evaluate_stream
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvaluateConsistencyRequestView"
      responses:
        "200":
          description: Stream SSE con progreso y reporte final
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/ConsistencyStreamEvent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /api/v1/projects/{project_id}/consistency/apply:
    post:
      tags: [consistency]
      summary: Aplicar impactos de consistencia
      description: Aplica las acciones sugeridas sobre requisitos, caracteristicas y diagramas.
      operationId: consistency_apply
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplyImpactsRequestView"
      responses:
        "200":
          description: Resultado de la aplicacion de impactos
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyImpactsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ CHAT SESSIONS ══════════════════════════════

  /api/v1/projects/{project_id}/chat-sessions:
    get:
      tags: [chat-sessions]
      summary: Listar sesiones de chat de una fase
      description: >-
        Devuelve los hilos de conversacion persistidos de una fase, del mas reciente al mas antiguo,
        con conteo de mensajes y fecha del ultimo.
      operationId: chat_sessions_list
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: phase
          in: query
          required: true
          schema:
            type: string
            enum: [discovery, features, requirements, model]
          description: Fase a listar
        - name: context_id
          in: query
          required: false
          schema:
            type: string
          description: Contexto opcional (id de caracteristica)
      responses:
        "200":
          description: Lista de sesiones de chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatSessionListView"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [chat-sessions]
      summary: Crear una sesion de chat nueva con contexto limpio
      description: Crea un hilo nuevo para la fase indicada conservando las sesiones anteriores.
      operationId: chat_sessions_create
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatSessionRequestView"
      responses:
        "201":
          description: Sesion de chat creada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatSessionDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/chat-sessions/{session_id}:
    delete:
      tags: [chat-sessions]
      summary: Eliminar una sesion de chat
      description: Elimina el hilo de conversacion y todos sus mensajes asociados.
      operationId: chat_sessions_delete
      parameters:
        - $ref: "#/components/parameters/ProjectId"
        - name: session_id
          in: path
          required: true
          schema:
            type: string
          description: Identificador de la sesion (cht_ + ULID)
      responses:
        "204":
          description: Sesion eliminada exitosamente. Sin cuerpo de respuesta.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ DOCUMENTS ══════════════════════════════

  /api/v1/documents/modify-direct:
    post:
      tags: [documents]
      summary: Modificar documento directamente sin fase de plan
      description: >-
        Recibe una instruccion textual y modifica directamente el documento indicado sin pasar por la fase de plan.
        La IA interpreta la intencion, identifica la seccion y retorna el documento actualizado.
      operationId: documents_modify_direct
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentModifyRequestView"
      responses:
        "200":
          description: Documento actualizado exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentModifyResponseView"
        "400":
          description: Instruccion ambigua o invalida
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/BadGateway"

  /api/v1/documents/revert:
    post:
      tags: [documents]
      summary: Revertir a una version anterior del documento
      description: Restaura una version previa del documento de descubrimiento.
      operationId: documents_revert
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RevertDocumentRequest"
      responses:
        "200":
          description: Documento revertido exitosamente
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  version_id:
                    type: string
                required: [status, version_id]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ══════════════════════════════ SCHEMAS ══════════════════════════════

  /api/v1/schemas:
    get:
      tags: [schemas]
      summary: Listar nombres de esquemas disponibles
      description: Devuelve el catalogo de DTOs disponibles para introspeccion de esquemas JSON.
      operationId: schemas_list
      responses:
        "200":
          description: Lista de esquemas registrados
          content:
            application/json:
              schema:
                type: object
                properties:
                  schemas:
                    type: array
                    items:
                      type: string
                required: [schemas]

  /api/v1/schemas/{name}:
    get:
      tags: [schemas]
      summary: Obtener JSON Schema de un DTO
      description: Devuelve el JSON Schema generado a partir del modelo Pydantic indicado.
      operationId: schemas_get
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
          description: Nombre del DTO (ej. RegisterRequest, CreateProjectRequest)
      responses:
        "200":
          description: JSON Schema del DTO
          content:
            application/json:
              schema:
                type: object
        "404":
          description: Esquema no encontrado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  # ══════════════════════════════ KNOWLEDGE ══════════════════════════════

  /api/v1/knowledge/consolidate:
    post:
      tags: [knowledge]
      summary: Consolidar patrones de conocimiento cross-project
      description: Consolida reflexiones y patrones de sesiones de agentes en memoria compartida cross-project.
      operationId: knowledge_consolidate
      responses:
        "200":
          description: Patrones consolidados por fase
          content:
            application/json:
              schema:
                type: object
                properties:
                  phases:
                    type: array
                    items:
                      type: object
                required: [phases]
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ══════════════════════════════ TRACEABILITY ══════════════════════════════

  /traceability/{entity_id}/navigation:
    get:
      tags: [Traceability]
      summary: Verificar navegacion de trazabilidad
      description: Verifica si la edicion esta permitida en este nivel de SDD o sugiere redireccion a la entidad fuente.
      operationId: traceability_navigation_check
      parameters:
        - name: entity_id
          in: path
          required: true
          schema:
            type: string
          description: Identificador de la entidad a verificar
        - name: level
          in: query
          required: true
          schema:
            type: string
            enum: [descubrimiento, caracteristicas, requisitos, modelo, codigo]
          description: Fase de la entidad
      responses:
        "200":
          description: Estado de navegacion y trazabilidad
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TraceabilityNavigationOutputView"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ══════════════════════════════ MCP TOOLS ══════════════════════════════

  /mcp/tools/get_requirements:
    post:
      tags: [MCP Tools]
      summary: Obtener requisitos EARS de una caracteristica
      description: Retorna los requisitos EARS en formato markdown para la caracteristica indicada (para agentes OpenCode).
      operationId: mcp_get_requirements
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MCPToolRequest"
      responses:
        "200":
          description: Requisitos EARS en Markdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MCPToolResponse"
        "404":
          description: Requisitos no encontrados para la caracteristica indicada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  /mcp/tools/get_activity_diagram:
    post:
      tags: [MCP Tools]
      summary: Obtener diagrama de actividad de una caracteristica
      description: Retorna el diagrama de actividad en formato PlantUML para la caracteristica indicada (para agentes OpenCode).
      operationId: mcp_get_activity_diagram
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MCPToolRequest"
      responses:
        "200":
          description: Diagrama de actividad en PlantUML
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MCPToolResponse"
        "404":
          description: Diagrama no encontrado para la caracteristica indicada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  # ══════════════════════════════ IMPLEMENTATIONS ══════════════════════════════

  /api/v1/implementations:
    post:
      tags: [Implementations]
      summary: Iniciar implementacion
      description: Inicia asincronamente la generacion de codigo fuente para una caracteristica mediante OpenCode.
      operationId: implementations_start
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateImplementationRequest"
      responses:
        "202":
          description: Implementacion iniciada asincronamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateImplementationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      tags: [Implementations]
      summary: Obtener implementacion por caracteristica
      description: >-
        Devuelve el registro persistido de la implementacion de una caracteristica (fuente de verdad para el frontend).
      operationId: implementations_get_by_feature
      parameters:
        - name: feature_id
          in: query
          required: true
          schema:
            type: string
            minLength: 1
          description: ID de la caracteristica
      responses:
        "200":
          description: Registro persistido de la implementacion
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImplementationRecordResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: No se encontro una implementacion para la caracteristica
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  /api/v1/implementations/{implementation_id}/events:
    get:
      tags: [Implementations]
      summary: Suscribirse a eventos de implementacion
      description: Flujo SSE para eventos en tiempo real emitidos durante la generacion de codigo.
      operationId: implementations_stream_events
      parameters:
        - name: implementation_id
          in: path
          required: true
          schema:
            type: string
          description: ID de la implementacion iniciada
      responses:
        "200":
          description: Stream SSE de eventos de implementacion
          content:
            text/event-stream:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/implementations/{implementation_id}/files/content:
    get:
      tags: [Implementations]
      summary: Leer contenido de un archivo generado
      description: Devuelve el contenido de un archivo del workspace de la implementacion, validando seguridad de ruta.
      operationId: implementations_get_file_content
      parameters:
        - name: implementation_id
          in: path
          required: true
          schema:
            type: string
          description: ID de la implementacion
        - name: path
          in: query
          required: true
          schema:
            type: string
            minLength: 1
          description: Ruta relativa del archivo dentro del workspace
      responses:
        "200":
          description: Contenido del archivo en texto plano
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImplementationFileContentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Implementacion, workspace o archivo no encontrado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"
        "422":
          description: Ruta insegura o fuera del workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpErrorResponse"

  /api/v1/implementations/{implementation_id}/validate:
    post:
      tags: [Implementations]
      summary: Validar workspace de implementación en contenedor efímero
      description: >-
        Ejecuta el pipeline de validación determinística (typecheck, linter, tests y build)
        sobre el workspace de la implementación dentro de un contenedor Docker efímero (kosmo-validator)
        que se destruye inmediatamente al finalizar, garantizando que el espacio de trabajo local
        permanezca limpio antes de la sincronización remota.
      operationId: implementations_validate_workspace
      parameters:
        - name: implementation_id
          in: path
          required: true
          schema:
            type: string
          description: ID de la implementación
      responses:
        "200":
          description: Resultado estructurado de todos los pasos de validación
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateWorkspaceResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Implementación o workspace no encontrado
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  # ══════════════════════════════ AI CONFIG (HU-25 BYOK) ══════════════════════════════

  /api/v1/user/ai-config:
    get:
      tags: [ai-config]
      summary: Obtener configuración del proveedor de IA del usuario
      description: >-
        Devuelve las preferencias de Inteligencia Artificial del usuario autenticado (proveedor,
        modelo y clave de API enmascarada). Si el usuario no tiene una clave personalizada configurada,
        indica que opera bajo el proveedor y modelo predeterminados de la plataforma KOSMO.
      operationId: ai_config_get
      responses:
        "200":
          description: Configuración de IA activa del usuario
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIConfigResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      tags: [ai-config]
      summary: Guardar o actualizar configuración del proveedor de IA
      description: >-
        Persiste la selección de proveedor y modelo de IA del usuario. Si se envía una nueva clave de API,
        el servidor la cifra simétricamente mediante Fernet antes de almacenarla en la base de datos.
        La respuesta devuelve la clave enmascarada mostrando únicamente los últimos 4 caracteres.
      operationId: ai_config_save
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveAIConfigRequest"
      responses:
        "200":
          description: Configuración de IA actualizada exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIConfigResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    delete:
      tags: [ai-config]
      summary: Eliminar credenciales personalizadas de IA
      description: >-
        Elimina la clave de API personalizada del usuario de la base de datos y restablece la generación
        de código al proveedor y modelo predeterminados de KOSMO.
      operationId: ai_config_delete
      responses:
        "200":
          description: Credenciales eliminadas y configuración restablecida a los valores predeterminados
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIConfigResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/user/ai-config/test:
    post:
      tags: [ai-config]
      summary: Probar conexión con proveedor de IA
      description: >-
        Ejecuta una llamada ligera de comprobación contra la API del proveedor de Inteligencia Artificial
        seleccionado utilizando la clave proporcionada (o la clave persistida del usuario si se omite).
        Valida la disponibilidad del servicio y confirma el identificador del modelo detectado sin persistir
        cambios en el perfil.
      operationId: ai_config_test_connection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestAIConnectionRequest"
      responses:
        "200":
          description: Resultado de la prueba de conexión
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestAIConnectionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "502":
          description: Error al comunicarse con la API del proveedor externo de IA (credencial inválida, permisos insuficientes o servicio no disponible)
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  # ══════════════════════════════ INTEGRATIONS (OAUTH) ══════════════════════════════

  /api/v1/integrations/{provider}/connect:
    post:
      tags: [integrations]
      summary: Vincular cuenta de plataforma externa mediante OAuth
      description: >-
        Procesa el código de autorización temporal emitido tras el flujo OAuth con la plataforma externa
        (github o railway), intercambia el código por el token de acceso correspondiente, lo cifra
        simétricamente con Fernet y lo asocia a la cuenta del usuario autenticado.
      operationId: integrations_connect
      parameters:
        - $ref: "#/components/parameters/IntegrationProvider"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConnectOAuthRequest"
      responses:
        "200":
          description: Cuenta vinculada exitosamente
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntegrationStatusResponse"
        "400":
          description: Código de autorización OAuth inválido o expirado
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "502":
          description: Error al comunicarse con el servidor OAuth de la plataforma externa
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  /api/v1/integrations/{provider}/status:
    get:
      tags: [integrations]
      summary: Consultar estado de vinculación de plataforma externa
      description: >-
        Verifica si el usuario autenticado tiene una cuenta vinculada activa para la plataforma especificada
        (github o railway), devolviendo los metadatos públicos de la conexión sin exponer credenciales sensibles.
      operationId: integrations_get_status
      parameters:
        - $ref: "#/components/parameters/IntegrationProvider"
      responses:
        "200":
          description: Estado de la vinculación con la plataforma externa
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntegrationStatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/integrations/{provider}:
    delete:
      tags: [integrations]
      summary: Desvincular plataforma externa
      description: >-
        Revoca y elimina las credenciales OAuth almacenadas de la plataforma externa especificada
        para la cuenta del usuario autenticado.
      operationId: integrations_disconnect
      parameters:
        - $ref: "#/components/parameters/IntegrationProvider"
      responses:
        "204":
          description: Plataforma externa desvinculada exitosamente. Sin cuerpo de respuesta.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: La integración no se encuentra vinculada
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  # ══════════════════════════════ GITHUB SYNC (HU-23) ══════════════════════════════

  /api/v1/projects/{project_id}/github:
    get:
      tags: [github]
      summary: Obtener estado de integración con GitHub del proyecto
      description: >-
        Devuelve los metadatos del repositorio remoto en GitHub asociado al proyecto, incluyendo la URL remota,
        visibilidad (privado/público), fecha de la última sincronización y hash del último commit enviado.
      operationId: project_github_get_status
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Estado de sincronización con GitHub del proyecto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGitHubResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/github/push:
    post:
      tags: [github]
      summary: Crear repositorio o sincronizar código en GitHub
      description: >-
        Orquesta la subida del código fuente del proyecto hacia GitHub.
        1. Ejecuta la validación previa del workspace en un contenedor efímero.
        2. Si el proyecto aún no tiene un repositorio asociado, lo crea vía API de GitHub con el nombre y visibilidad
           indicados (privado por defecto o público previa confirmación) y configura el remote origin.
        3. Si el repositorio ya existe, realiza un push incremental con los nuevos commits.
        4. Actualiza y persiste los metadatos de sincronización en la base de datos.
      operationId: project_github_push
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushGitHubRequest"
      responses:
        "200":
          description: Código sincronizado exitosamente con el repositorio en GitHub
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGitHubResponse"
        "400":
          description: Parámetros de repositorio inválidos o nombre no disponible
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Conflicto: el usuario no ha vinculado su cuenta de GitHub o el proyecto no tiene funcionalidades implementadas"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "422":
          description: Fallo de validación en contenedor efímero antes de subir el código
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "502":
          description: Error al comunicarse con la API de GitHub o al ejecutar la operación Git remota
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

  # ══════════════════════════════ DEPLOY RAILWAY (HU-24) ══════════════════════════════

  /api/v1/projects/{project_id}/deploy:
    get:
      tags: [deploy]
      summary: Obtener estado de despliegue en la nube
      description: >-
        Consulta el estado de publicación del proyecto en Railway (idle, pending, building, ready, failed),
        la URL pública accesible en internet, marca de tiempo del último despliegue y enlace a logs en caso de fallo.
      operationId: project_deploy_get_status
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      responses:
        "200":
          description: Estado actual del despliegue del proyecto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDeployStatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{project_id}/deploy/railway:
    post:
      tags: [deploy]
      summary: Iniciar despliegue del proyecto en Railway
      description: >-
        Orquesta la publicación de la aplicación en la nube mediante Railway:
        1. Valida que el proyecto cuente con un repositorio GitHub sincronizado y que la cuenta de Railway esté vinculada.
        2. Crea o actualiza el servicio en Railway conectado al repositorio remoto.
        3. Configura el volumen persistente para SQLite (/data/db.sqlite) y variables de entorno requeridas.
        4. Dispara la construcción del contenedor a partir del Dockerfile de producción y actualiza el estado a 'building'.
      operationId: project_deploy_railway_start
      parameters:
        - $ref: "#/components/parameters/ProjectId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeployRailwayRequest"
      responses:
        "202":
          description: Despliegue iniciado asíncronamente en la plataforma de Railway
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDeployStatusResponse"
        "400":
          description: Configuración de despliegue inválida
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "Precondición no cumplida: el proyecto no tiene repositorio en GitHub o la cuenta de Railway no está vinculada"
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "502":
          description: Error al interactuar con la API de Railway
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT RS256 emitido por /api/v1/auth/token o /api/v1/auth/refresh

  parameters:
    ProjectId:
      name: project_id
      in: path
      required: true
      schema:
        type: string
      description: Identificador del proyecto (prj_ + ULID)
    FeatureId:
      name: feature_id
      in: path
      required: true
      schema:
        type: string
      description: Identificador o slug de la característica
    ProjectIdQuery:
      name: project_id
      in: query
      required: true
      schema:
        type: string
      description: Identificador del proyecto
    CursorBefore:
      name: before
      in: query
      required: false
      schema:
        type: string
      description: Cursor ISO-8601 para paginación hacia atrás
    SessionIdQuery:
      name: session_id
      in: query
      required: false
      schema:
        type: string
      description: Hilo de conversación (cht_ + ULID). Si se omite, contexto de la fase.
    IntegrationProvider:
      name: provider
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/IntegrationProviderEnum"
      description: Identificador del proveedor de integración externa (github | railway)

  responses:
    Unauthorized:
      description: Token de acceso ausente, invalido, expirado o revocado
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/HttpErrorResponse"
    NotFound:
      description: Recurso no encontrado
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetail"
    BadRequest:
      description: Peticion invalida o error de validacion
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetail"
    BadGateway:
      description: Error al invocar el servicio de IA o LLM
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetail"
    InternalServerError:
      description: Error interno del servidor
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/HttpErrorResponse"
    UnprocessableEntity:
      description: Contenido no procesable o violacion de reglas semanticas
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ProblemDetail"

  schemas:
    ProblemDetail:
      type: object
      description: Error RFC 7807 (application/problem+json)
      properties:
        type:
          type: string
          description: URN del error (ej. urn:kosmo:projects:not-found)
          example: urn:kosmo:projects:not-found
        title:
          type: string
          description: Titulo corto del error
        status:
          type: integer
          description: Codigo de estado HTTP
        detail:
          type: string
          description: Descripcion detallada del error
        instance:
          type: string
          description: Ruta o recurso que origino el error
        trace_id:
          type: string
          description: Identificador de correlacion distribuida (X-Trace-Id)
        violations:
          type: array
          items:
            type: object
            properties:
              loc:
                type: string
                description: Ruta al campo invalido
              msg:
                type: string
                description: Mensaje de error de validacion
              input:
                description: Valor recibido que causo la violacion
      required: [type, title, status, detail]

    HttpErrorResponse:
      type: object
      description: Respuesta de error generica para errores HTTP (4xx/5xx)
      properties:
        detail:
          type: string
          description: Mensaje descriptivo del error
          example: Recurso no encontrado.
      required: [detail]

    # ── Auth ──

    RegisterRequest:
      type: object
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          description: Direccion de correo electronico unica
          example: usuario@ejemplo.com
        password:
          type: string
          minLength: 12
          maxLength: 128
          description: Contrasena en texto plano (se hashea con Argon2id)
          example: "M1ContrasenaSegura!"
      required: [email, password]

    AuthorizeRequest:
      type: object
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          description: Email del usuario
          example: usuario@ejemplo.com
        password:
          type: string
          minLength: 1
          maxLength: 128
          description: Contrasena en texto plano
          example: "M1ContrasenaSegura!"
        code_challenge:
          type: string
          minLength: 43
          maxLength: 128
          description: Hash SHA-256 del code_verifier en Base64URL sin padding
          example: E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM
        code_challenge_method:
          type: string
          enum: [S256]
          default: S256
          description: Metodo de transformacion PKCE
          example: S256
        scopes:
          type: array
          items:
            type: string
          default: []
          description: Permisos solicitados para la sesion
          example: ["profile:read", "agent:run"]
      required: [email, password, code_challenge]

    TokenExchangeRequest:
      type: object
      additionalProperties: false
      properties:
        grant_type:
          type: string
          enum: [authorization_code]
          description: Debe ser exactamente 'authorization_code'
          example: authorization_code
        code:
          type: string
          minLength: 1
          maxLength: 256
          description: Codigo de autorizacion opaco devuelto por /authorize
          example: a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2
        code_verifier:
          type: string
          minLength: 43
          maxLength: 128
          description: Secreto original generado por el cliente
          example: dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk
      required: [grant_type, code, code_verifier]

    TokenRefreshRequest:
      type: object
      additionalProperties: false
      properties:
        grant_type:
          type: string
          enum: [refresh_token]
          description: Debe ser exactamente 'refresh_token'
          example: refresh_token
        refresh_token:
          type: string
          minLength: 1
          description: JWT de tipo refresh emitido previamente
      required: [grant_type, refresh_token]

    LogoutRequest:
      type: object
      additionalProperties: false
      properties:
        refresh_token:
          type: string
          nullable: true
          description: JWT de tipo refresh a revocar junto con el access token
      required: []

    UserPublic:
      type: object
      description: Datos publicos del usuario registrado
      properties:
        id:
          type: string
          description: Identificador unico (usr_ + ULID)
          example: usr_01HXYAZABCDEFGHIJKLMNOP
        email:
          type: string
          format: email
          example: usuario@ejemplo.com
        created_at:
          type: string
          format: date-time
          example: "2026-01-15T10:30:00Z"
      required: [id, email, created_at]

    AuthorizationCodeResponse:
      type: object
      properties:
        authorization_code:
          type: string
          description: Codigo de autorizacion opaco de un solo uso
          example: a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2
        expires_in:
          type: integer
          minimum: 1
          maximum: 600
          description: Segundos hasta expiracion
          example: 300
      required: [authorization_code, expires_in]

    TokenView:
      type: object
      properties:
        token:
          type: string
          description: Token JWT firmado con RS256
        jti:
          type: string
          description: JWT ID unico para revocacion en Redis
          example: ati-01HXYAZABCDEFGHIJKLMNOP
        expires_at:
          type: string
          format: date-time
          example: "2026-12-31T23:59:59Z"
      required: [token, jti, expires_at]

    TokenPairResponse:
      type: object
      properties:
        access:
          $ref: "#/components/schemas/TokenView"
        refresh:
          $ref: "#/components/schemas/TokenView"
        token_type:
          type: string
          enum: [Bearer]
          default: Bearer
          example: Bearer
      required: [access, refresh, token_type]

    PrincipalView:
      type: object
      properties:
        subject:
          type: string
          description: Identificador del usuario propietario del token
          example: usr_01HXYAZABCDEFGHIJKLMNOP
        scopes:
          type: array
          items:
            type: string
          description: Permisos concedidos en esta sesion
          example: ["agent:run", "profile:read"]
      required: [subject, scopes]

    OAuthErrorResponse:
      type: object
      description: Respuesta de error compatible con RFC 6749 seccion 5.2
      properties:
        error:
          type: string
          description: Codigo de error maquina-legible
          example: invalid_grant
        error_description:
          type: string
          description: Descripcion legible del error
          example: Credenciales invalidas
      required: [error, error_description]

    # ── Projects ──

    CreateProjectRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          description: Nombre del proyecto
          example: Mi Proyecto
        description:
          type: string
          maxLength: 2000
          default: ""
          description: Descripcion del proyecto
          example: Proyecto de ejemplo para KOSMO
      required: [name]

    ProjectResponse:
      type: object
      properties:
        id:
          type: string
          description: Identificador unico del proyecto (prj_ + ULID)
          example: prj_01HXYAZABCDEFGHIJKLMNOP
        name:
          type: string
          example: Mi Proyecto
        slug:
          type: string
          example: mi-proyecto
        description:
          type: string
          example: Proyecto de ejemplo para KOSMO
        owner_id:
          type: string
          example: usr_01HXYAZABCDEFGHIJKLMNOP
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required: [id, name, slug, description, owner_id, created_at, updated_at]

    ProjectPreviewResponse:
      type: object
      properties:
        url:
          type: string
          description: URL publica de la vista previa del proyecto
          example: http://localhost:5173
      required: [url]

    # ── Discovery ──

    DiscoveryResponse:
      type: object
      properties:
        id:
          type: string
          example: doc_prj_01HXYAZABCDEFGHIJKLMNOP
        project_id:
          type: string
          example: prj_01HXYAZABCDEFGHIJKLMNOP
        content:
          type: string
          description: Contenido del documento en formato Markdown
      required: [id, project_id, content]

    SaveDiscoveryRequest:
      type: object
      properties:
        content:
          type: string
          description: Contenido del documento en formato Markdown
      required: [content]

    RefineDiscoveryRequest:
      type: object
      additionalProperties: false
      properties:
        instructions:
          type: string
          minLength: 1
          maxLength: 500
          description: Instrucciones de refinamiento para el agente IA
          example: Haz que la vision del producto sea mas concisa y enfocada en resultados.
      required: [instructions]

    # ── Features ──

    FeatureResponse:
      type: object
      properties:
        id:
          type: string
          description: Identificador unico de la caracteristica
          example: feat_01HXYAZABCDEFGHIJKLMNOP
        project_id:
          type: string
          example: prj_01HXYAZABCDEFGHIJKLMNOP
        number:
          type: integer
          example: 1
        title:
          type: string
          example: Gestion de catalogo de productos
        slug:
          type: string
          example: gestion-de-catalogo-de-productos
        description:
          type: string
          example: Permite a los usuarios administrar el catalogo de productos.
        origin:
          type: string
          example: Se deriva de la seccion Capacidades del descubrimiento.
        display_id:
          type: string
          example: C01
      required: [id, project_id, number, title, slug, description, origin, display_id]

    FeatureSuggestionItem:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        origin:
          type: string
          default: ""
      required: [title, description]

    FeatureSuggestionResponse:
      type: object
      properties:
        number:
          type: integer
          example: 1
        title:
          type: string
          example: Modulo de facturacion electronica
        description:
          type: string
          example: Generacion y envio de comprobantes tributarios.
        origin:
          type: string
          example: Derivado de Reglas de Negocio en Descubrimiento.
      required: [number, title, description, origin]

    SaveSelectedFeaturesRequest:
      type: object
      properties:
        features:
          type: array
          items:
            $ref: "#/components/schemas/FeatureSuggestionItem"
      required: [features]

    CreateCharacteristicRequest:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 50
          description: Titulo de la caracteristica (maximo 50 caracteres)
          example: Gestion de catalogo de productos
        description:
          type: string
          maxLength: 500
          default: ""
          description: Descripcion de la caracteristica (maximo 500 caracteres)
          example: Permite a los usuarios administrar el catalogo de productos.
        origin:
          type: string
          default: ""
          description: Origen y trazabilidad. Si se omite, la IA lo deriva.
      required: [title]

    EditFeatureManualRequest:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 50
          description: Nuevo titulo de la caracteristica
        description:
          type: string
          minLength: 0
          maxLength: 500
          description: Nueva descripcion de la caracteristica
      required: [title, description]

    CreateCharacteristicResult:
      type: object
      properties:
        is_saved:
          type: boolean
          description: True si la caracteristica fue guardada
        feature:
          $ref: "#/components/schemas/FeatureResponse"
          description: Presente solo si is_saved=true
        origin:
          type: string
        is_consistent:
          type: boolean
          description: False si se detecto contradiccion con el descubrimiento
        inconsistency_reason:
          type: string
          nullable: true
          description: Presente solo si is_saved=false por inconsistencia
      required: [is_saved, origin, is_consistent]

    CheckConsistencyRequestView:
      type: object
      additionalProperties: false
      properties:
        content:
          type: object
          description: Contenido nuevo a validar contra el documento fuente
      required: [content]

    InconsistencyResultView:
      type: object
      properties:
        is_consistent:
          type: boolean
          description: False si se detecto contradiccion
        reason:
          type: string
          nullable: true
          description: Motivo de la inconsistencia
        conflicting_section:
          type: string
          nullable: true
          description: Seccion del documento fuente con conflicto
      required: [is_consistent]

    # ── Chat ──

    SendChatRequest:
      type: object
      additionalProperties: false
      properties:
        content:
          type: string
          minLength: 1
          maxLength: 4000
          description: Mensaje textual del usuario
        session_id:
          type: string
          nullable: true
          description: Hilo de conversacion (cht_ + ULID). Si se omite, contexto de la fase.
      required: [content]

    ChangeSuggestion:
      type: object
      description: Sugerencia de cambio generada por la IA
      properties:
        id:
          type: string
          description: ID de la sugerencia (chg_ + ULID)
        section:
          type: string
          description: Seccion del documento afectada
        description:
          type: string
          description: Descripcion corta del cambio propuesto
        diff_before:
          type: string
          description: Contenido actual de la seccion
        diff_after:
          type: string
          description: Contenido sugerido por la IA
        rationale:
          type: string
          nullable: true
          description: Justificacion del cambio propuesto
        applied:
          type: boolean
          default: true
          description: True si el servidor aplico el cambio al instante
        not_applied_reason:
          type: string
          nullable: true
          description: Razon por la que no se pudo aplicar
      required: [id, section, description, diff_before, diff_after, applied]

    ModificationResultView:
      type: object
      description: Resultado de la modificacion aplicada por el chat
      properties:
        applied:
          type: boolean
          description: True si la modificacion fue aplicada al documento
        modified_section:
          type: string
          nullable: true
          description: Nombre de la seccion o atributo modificado
        change_description:
          type: string
          nullable: true
          description: Descripcion breve del cambio aplicado
        modified_document:
          type: string
          nullable: true
          description: Markdown completo actualizado
        before:
          type: string
          nullable: true
          description: Contenido previo modificado
        after:
          type: string
          nullable: true
          description: Contenido posterior modificado
        undo_version_id:
          type: string
          nullable: true
          description: Version previa al cambio (reservado para undo)
        clarification_message:
          type: string
          nullable: true
          description: Mensaje solicitando detalle cuando la instruccion es ambigua
      required: [applied]

    RedirectInfoView:
      type: object
      description: Redireccion cuando el cambio pertenece a otra fase upstream
      properties:
        target_phase:
          type: string
          description: Fase destino del cambio
        redirect_message:
          type: string
          description: Explicacion de por que el cambio pertenece a esa fase
      required: [target_phase, redirect_message]

    ChatMessage:
      type: object
      properties:
        id:
          type: string
          description: ID del mensaje
        role:
          type: string
          description: Rol del emisor (user, assistant, system)
        content:
          type: string
          description: Contenido del mensaje
        created_at:
          type: string
          format: date-time
        change_suggestions:
          type: array
          nullable: true
          items:
            $ref: "#/components/schemas/ChangeSuggestion"
        modification:
          $ref: "#/components/schemas/ModificationResultView"
      required: [id, role, content, created_at]

    ChatResponse:
      type: object
      description: Respuesta del chat con modificacion aplicada, consistencia o redireccion
      properties:
        message:
          $ref: "#/components/schemas/ChatMessage"
        modification:
          $ref: "#/components/schemas/ModificationResultView"
        redirect:
          $ref: "#/components/schemas/RedirectInfoView"
        consistency:
          type: array
          nullable: true
          items:
            type: object
          description: Impactos de consistencia en las fases downstream
      required: [message]

    ChatHistoryResponse:
      type: object
      properties:
        phase:
          type: string
        context:
          type: string
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ChatMessage"
        has_more:
          type: boolean
          default: false
        next_cursor:
          type: string
          nullable: true
      required: [phase, context, messages]

    ChatStreamEvent:
      oneOf:
        - type: object
          description: Inicio del stream
          properties:
            type:
              type: string
              const: start
          required: [type]
        - type: object
          description: Fragmento de texto en tiempo real
          properties:
            type:
              type: string
              const: chunk
            content:
              type: string
          required: [type, content]
        - type: object
          description: Mensaje final completo con cards de cambio
          properties:
            type:
              type: string
              const: message
            id:
              type: string
            role:
              type: string
            content:
              type: string
            suggestions:
              type: array
              items:
                $ref: "#/components/schemas/ChangeSuggestion"
            modification:
              $ref: "#/components/schemas/ModificationResultView"
            timestamp:
              type: string
              format: date-time
          required: [type, id, role, content]

    # ── Chat Sessions ──

    CreateChatSessionRequestView:
      type: object
      additionalProperties: false
      properties:
        phase:
          type: string
          description: Fase del chat (discovery, features, requirements, model)
        context_id:
          type: string
          nullable: true
          description: Contexto de la fase (id de caracteristica; null para discovery)
      required: [phase]

    ChatSessionItem:
      type: object
      properties:
        id:
          type: string
        phase:
          type: string
        context_id:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        message_count:
          type: integer
          default: 0
        last_message_at:
          type: string
          format: date-time
          nullable: true
        title:
          type: string
          default: ""
      required: [id, phase, created_at]

    ChatSessionDetailResponse:
      type: object
      properties:
        id:
          type: string
        session_id:
          type: string
        phase:
          type: string
        context_id:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        message_count:
          type: integer
          default: 0
        last_message_at:
          type: string
          format: date-time
          nullable: true
        title:
          type: string
          default: ""
      required: [id, session_id, phase, created_at]

    ChatSessionListView:
      type: object
      properties:
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/ChatSessionItem"
      required: [sessions]

    # ── Requirements ──

    GenerateRequirementsRequest:
      type: object
      additionalProperties: false
      properties:
        project_id:
          type: string
          description: ID del proyecto
      required: [project_id]

    EARSRequirementView:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        statement:
          type: string
        pattern:
          type: string
          description: Patron EARS (ubiquitous, event_driven, state_driven, optional, unwanted, complex)
        acceptance_criteria:
          type: array
          items:
            type: object
      required: [id, title, statement, pattern]

    GenerateRequirementsResponse:
      type: object
      properties:
        project_id:
          type: string
        feature_id:
          type: string
        feature_number:
          type: integer
        document_markdown:
          type: string
        total:
          type: integer
        requirements:
          type: array
          items:
            $ref: "#/components/schemas/EARSRequirementView"
      required: [project_id, feature_id, feature_number, document_markdown, total, requirements]

    RequirementsDocumentResponse:
      type: object
      properties:
        document_markdown:
          type: string
        total:
          type: integer
      required: [document_markdown, total]

    SaveRequirementsRequest:
      type: object
      additionalProperties: false
      properties:
        project_id:
          type: string
        markdown:
          type: string
      required: [project_id, markdown]

    RefineRequirementsRequest:
      type: object
      additionalProperties: false
      properties:
        project_id:
          type: string
        instructions:
          type: string
          minLength: 1
          maxLength: 500
      required: [project_id, instructions]

    RefineRequirementsResponse:
      type: object
      properties:
        feature_id:
          type: string
        feature_number:
          type: integer
        document_markdown:
          type: string
        total:
          type: integer
      required: [feature_id, feature_number, document_markdown, total]

    RegenerateRequirementsResponse:
      type: object
      properties:
        artifact_id:
          type: string
        content:
          type: string
        phase:
          type: string
      required: [artifact_id, content, phase]

    # ── Modelo ──

    GenerateDiagramRequest:
      type: object
      additionalProperties: false
      properties:
        project_id:
          type: string
      required: [project_id]

    DiagramResponse:
      type: object
      properties:
        id:
          type: string
        feature_id:
          type: string
        diagram_syntax:
          type: string
          description: Codigo fuente PlantUML
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required: [id, feature_id, diagram_syntax, created_at, updated_at]

    # ── Consistency ──

    ConsistencyPhaseCounts:
      type: object
      properties:
        pending:
          type: integer
          description: Sugerencias completadas sin resolver
        evaluating:
          type: integer
          description: Evaluaciones en curso
        failed:
          type: integer
          description: Analisis fallidos
      required: [pending, evaluating, failed]

    ConsistencyStatusView:
      type: object
      properties:
        phases:
          type: object
          properties:
            features:
              $ref: "#/components/schemas/ConsistencyPhaseCounts"
            requirements:
              $ref: "#/components/schemas/ConsistencyPhaseCounts"
            model:
              $ref: "#/components/schemas/ConsistencyPhaseCounts"
          required: [features, requirements, model]
      required: [phases]

    ReviewCardDiff:
      type: object
      properties:
        field:
          type: string
        before:
          type: string
        after:
          type: string
      required: [field, before, after]

    ReviewCardView:
      type: object
      properties:
        evaluation_id:
          type: string
        source_phase:
          type: string
        target_phase:
          type: string
        target_artifact_id:
          type: string
        artifact_type:
          type: string
        target_display_id:
          type: string
        target_title:
          type: string
        section:
          type: string
        rationale:
          type: string
        action:
          type: string
          enum: [update, delete, keep, create]
        diff:
          $ref: "#/components/schemas/ReviewCardDiff"
        status:
          type: string
          enum: [evaluating, completed, failed]
        failure_reason:
          type: string
          nullable: true
      required: [evaluation_id, source_phase, target_phase, target_artifact_id, artifact_type, status]

    ConsistencyReviewView:
      type: object
      properties:
        cards:
          type: array
          items:
            $ref: "#/components/schemas/ReviewCardView"
      required: [cards]

    ApplyEvaluationResult:
      type: object
      properties:
        evaluation_id:
          type: string
        applied:
          type: boolean
        discarded:
          type: boolean
        target_id:
          type: string
        project_id:
          type: string
      required: [evaluation_id, project_id]

    BulkResolveRequestView:
      type: object
      additionalProperties: false
      properties:
        action:
          type: string
          enum: [apply, discard]
          description: Accion a ejecutar (apply | discard)
        target_phase:
          type: string
          enum: [features, requirements, model]
          description: Fase destino a resolver
      required: [action, target_phase]

    BulkResolveResponse:
      type: object
      properties:
        resolved:
          type: integer
          description: Numero de sugerencias resueltas
        skipped:
          type: integer
          description: Numero de sugerencias omitidas
      required: [resolved, skipped]

    ActivityItemView:
      type: object
      properties:
        evaluation_id:
          type: string
        status:
          type: string
          enum: [applied, discarded]
        source_phase:
          type: string
        target_phase:
          type: string
        target_artifact_id:
          type: string
        target_title:
          type: string
        failure_reason:
          type: string
          nullable: true
        updated_at:
          type: string
          format: date-time
      required: [evaluation_id, status, source_phase, target_phase, target_artifact_id]

    ConsistencyActivityView:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ActivityItemView"
      required: [items]

    ChangeInputView:
      type: object
      properties:
        section:
          type: string
        diff_before:
          type: string
        diff_after:
          type: string
        description:
          type: string
          default: ""
      required: [section, diff_before, diff_after]

    EvaluateConsistencyRequestView:
      type: object
      additionalProperties: false
      properties:
        phase_origin:
          type: string
          description: Fase de origen (discovery, features, requirements, model)
        phase_destination:
          type: string
          nullable: true
          description: Fase destino downstream a evaluar
        changes:
          type: array
          items:
            $ref: "#/components/schemas/ChangeInputView"
      required: [phase_origin, changes]

    ImpactItemView:
      type: object
      properties:
        id:
          type: string
        phase:
          type: string
        targetId:
          type: string
        artifact_type:
          type: string
        targetDisplayId:
          type: string
        targetTitle:
          type: string
        section:
          type: string
          default: ""
        rationale:
          type: string
          default: ""
        diff:
          type: object
          nullable: true
        action:
          type: string
          default: update
      required: [id, phase, targetId, artifact_type]

    ConsistencyReportView:
      type: object
      properties:
        report_id:
          type: string
          description: ID del reporte (cnr_ + ULID)
        source_type:
          type: string
        source_id:
          type: string
        your_changes:
          type: array
          items:
            type: object
            properties:
              change_id:
                type: string
              section:
                type: string
              description:
                type: string
              diff:
                type: object
                properties:
                  before:
                    type: string
                  after:
                    type: string
              accepted:
                type: boolean
            required: [change_id, section, diff, accepted]
        upstream_impact:
          type: array
          items:
            $ref: "#/components/schemas/ImpactItemView"
        downstream_impact:
          type: array
          items:
            $ref: "#/components/schemas/ImpactItemView"
      required: [report_id, source_type, source_id, your_changes, upstream_impact, downstream_impact]

    ConsistencyStreamEvent:
      oneOf:
        - type: object
          description: Progreso de evaluacion
          properties:
            type:
              type: string
              const: progress
            message:
              type: string
          required: [type, message]
        - type: object
          description: Reporte final
          properties:
            type:
              type: string
              const: report
            report:
              $ref: "#/components/schemas/ConsistencyReportView"
          required: [type, report]

    ApplyImpactsRequestView:
      type: object
      additionalProperties: false
      properties:
        impacts:
          type: array
          items:
            type: object
            description: Diccionario con los datos del impacto a aplicar
      required: [impacts]

    ApplyImpactsResponse:
      type: object
      properties:
        applied:
          type: array
          items:
            type: object
            properties:
              target_id:
                type: string
              artifact_type:
                type: string
            required: [target_id, artifact_type]
        failed:
          type: array
          items:
            type: object
            properties:
              target_id:
                type: string
              artifact_type:
                type: string
              reason:
                type: string
            required: [target_id, artifact_type, reason]
      required: [applied, failed]

    # ── Documents ──

    DocumentModifyRequestView:
      type: object
      additionalProperties: false
      properties:
        document_type:
          type: string
          enum: [discovery, features, requirements, model]
          description: Tipo de documento a modificar
        document_id:
          type: string
          description: ID del documento o caracteristica a modificar
        instruction:
          type: string
          minLength: 1
          maxLength: 2000
          description: Instruccion textual del cambio deseado
      required: [document_type, document_id, instruction]

    DocumentModifyResponseView:
      type: object
      properties:
        document_id:
          type: string
          description: ID del documento modificado
        content:
          type: string
          description: Contenido completo del documento actualizado
        highlighted_section:
          type: string
          nullable: true
          description: Nombre de la seccion modificada
        message:
          type: string
          description: Mensaje de confirmacion
      required: [document_id, content, message]

    RevertDocumentRequest:
      type: object
      additionalProperties: false
      properties:
        project_id:
          type: string
        version_id:
          type: string
      required: [project_id, version_id]

    # ── Traceability ──

    TraceabilityNavigationOutputView:
      type: object
      properties:
        permitted:
          type: boolean
          description: Indica si la edicion esta permitida en este nivel
        redirect_message:
          type: string
          nullable: true
          description: Mensaje para mostrar al usuario si no esta permitido
        source_entity_name:
          type: string
          nullable: true
          description: Nombre de la entidad de origen
        source_entity_id:
          type: string
          nullable: true
          description: ID de la entidad de origen
        source_level:
          type: string
          nullable: true
          description: Fase de la entidad de origen
      required: [permitted]

    # ── MCP Tools ──

    MCPToolRequest:
      type: object
      properties:
        feature_id:
          type: string
          minLength: 1
          description: Identificador de la caracteristica
      required: [feature_id]

    MCPToolResponse:
      type: object
      properties:
        feature_id:
          type: string
        content:
          type: string
      required: [feature_id, content]

    # ── Implementations (Codegen) ──

    GenerateImplementationRequest:
      type: object
      properties:
        feature_id:
          type: string
          description: ID de la caracteristica a implementar
        max_retries:
          type: integer
          minimum: 1
          maximum: 5
          default: 3
          description: Numero maximo de reintentos
      required: [feature_id]

    GenerateImplementationResponse:
      type: object
      properties:
        implementation_id:
          type: string
          description: ID del proceso iniciado para consumir sus eventos
      required: [implementation_id]

    ImplementationFileContentResponse:
      type: object
      properties:
        path:
          type: string
          description: Ruta relativa del archivo solicitado
        content:
          type: string
          description: Contenido del archivo en texto plano
      required: [path, content]

    ImplementationRecordResponse:
      type: object
      description: Registro persistido de una implementacion (fuente de verdad para el frontend)
      properties:
        implementation_id:
          type: string
          description: ID de la implementacion
        feature_id:
          type: string
          description: ID de la caracteristica implementada
        project_id:
          type: string
          description: ID del proyecto al que pertenece la caracteristica
        status:
          type: string
          description: Estado actual de la implementacion
        generated_files:
          type: array
          items:
            type: string
          default: []
          description: Archivos generados en el workspace
        updated_at:
          type: string
          format: date-time
          description: Ultima actualizacion del registro
      required: [implementation_id, feature_id, project_id, status, generated_files, updated_at]

    # ── Validación Efímera (HU-23 CA-05 / Codegen) ──

    ValidationStepResultResponse:
      type: object
      description: Resultado de la ejecución de un paso del pipeline de validación determinística
      properties:
        step:
          type: string
          description: Identificador del paso ejecutado (typecheck, lint, tests, build)
          example: typecheck
        success:
          type: boolean
          description: Indica si el paso concluyó satisfactoriamente sin errores
          example: true
        duration_ms:
          type: integer
          description: Duración de ejecución del paso en milisegundos
          example: 1420
        exit_code:
          type: integer
          description: Código de salida retornado por el proceso en el contenedor efímero
          example: 0
        error_messages:
          type: array
          items:
            type: string
          default: []
          description: Mensajes de error estructurados extraídos de la salida estándar o error
          example: []
      required: [step, success, duration_ms, exit_code, error_messages]

    ValidateWorkspaceResponse:
      type: object
      description: Resultado integral del pipeline de validación en contenedor efímero
      properties:
        all_passed:
          type: boolean
          description: Indica si la totalidad de pasos de compilación y pruebas fueron exitosos
          example: true
        steps:
          type: array
          items:
            $ref: "#/components/schemas/ValidationStepResultResponse"
          description: Detalle de ejecución de cada uno de los pasos de validación
        failed_step:
          type: string
          nullable: true
          description: Nombre del primer paso que falló en caso de validación no exitosa
          example: null
        error_summary:
          type: array
          items:
            type: string
          default: []
          description: Resumen de errores consolidados para diagnóstico o retroalimentación a la IA
          example: []
        total_duration_ms:
          type: integer
          description: Duración total acumulada de la validación en milisegundos
          example: 4530
      required: [all_passed, steps, total_duration_ms]

    # ── AI Configuration (HU-25 BYOK) ──

    AIProviderEnum:
      type: string
      enum: [openai, anthropic, google, openrouter, custom, kosmo_default]
      description: Identificador del proveedor de Inteligencia Artificial
      example: anthropic

    AIConfigResponse:
      type: object
      description: Estado y configuración del proveedor de IA del usuario
      properties:
        provider:
          $ref: "#/components/schemas/AIProviderEnum"
        model:
          type: string
          description: Nombre del modelo de IA configurado
          example: claude-3-5-sonnet-20241022
        is_custom:
          type: boolean
          description: True si el usuario utiliza su propia clave de acceso; False si opera con el modelo predeterminado de KOSMO
          example: true
        has_api_key:
          type: boolean
          description: Indica si existe una clave de API guardada y cifrada en el sistema
          example: true
        masked_key:
          type: string
          nullable: true
          description: Clave enmascarada preservando únicamente los últimos cuatro caracteres para identificación visual
          example: "••••••••1234"
        updated_at:
          type: string
          format: date-time
          nullable: true
          description: Marca temporal ISO-8601 UTC de la última modificación de la configuración
          example: "2026-08-20T15:30:00Z"
      required: [provider, model, is_custom, has_api_key]

    SaveAIConfigRequest:
      type: object
      additionalProperties: false
      description: Payload para guardar o actualizar la configuración del proveedor de IA
      properties:
        provider:
          $ref: "#/components/schemas/AIProviderEnum"
        model:
          type: string
          minLength: 1
          maxLength: 100
          description: Identificador del modelo soportado por el proveedor
          example: claude-3-5-sonnet-20241022
        api_key:
          type: string
          minLength: 1
          maxLength: 500
          description: Clave secreta de API del proveedor (se cifra simétricamente con Fernet)
          example: "sk-ant-api03-abcdef1234567890"
      required: [provider, model, api_key]

    TestAIConnectionRequest:
      type: object
      additionalProperties: false
      description: Payload para probar la conectividad y validez de credenciales con el proveedor de IA
      properties:
        provider:
          $ref: "#/components/schemas/AIProviderEnum"
        model:
          type: string
          minLength: 1
          maxLength: 100
          description: Modelo a verificar con el proveedor
          example: claude-3-5-sonnet-20241022
        api_key:
          type: string
          minLength: 1
          maxLength: 500
          nullable: true
          description: Clave de API a verificar. Si se omite, se prueba con la clave actualmente persistida del usuario.
          example: "sk-ant-api03-abcdef1234567890"
      required: [provider, model]

    TestAIConnectionResponse:
      type: object
      description: Respuesta cualitativa de la verificación de conexión con el proveedor de IA
      properties:
        is_connected:
          type: boolean
          description: True si la conexión y autenticación con la API del proveedor fueron exitosas
          example: true
        detected_model:
          type: string
          description: Identificador del modelo confirmado por el proveedor externo
          example: claude-3-5-sonnet-20241022
        message:
          type: string
          description: Mensaje descriptivo del resultado de la prueba
          example: "Conexión exitosa con Anthropic. Modelo claude-3-5-sonnet-20241022 verificado y listo."
      required: [is_connected, detected_model, message]

    # ── Integrations (OAuth) ──

    IntegrationProviderEnum:
      type: string
      enum: [github, railway]
      description: Identificador de la plataforma externa para integración
      example: github

    ConnectOAuthRequest:
      type: object
      additionalProperties: false
      description: Payload para intercambiar el código temporal de OAuth por credenciales de acceso
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 500
          description: Código de autorización temporal devuelto por el proveedor OAuth
          example: "gho_16C7e42F292c6912E7710c838347Ae178B4a"
        redirect_uri:
          type: string
          nullable: true
          description: URI de redirección utilizada durante el flujo de autorización
          example: "http://localhost:3000/settings"
      required: [code]

    IntegrationStatusResponse:
      type: object
      description: Estado de vinculación de una plataforma externa con la cuenta de usuario
      properties:
        provider:
          $ref: "#/components/schemas/IntegrationProviderEnum"
        is_connected:
          type: boolean
          description: True si la cuenta de usuario se encuentra vinculada activamente con la plataforma
          example: true
        username:
          type: string
          nullable: true
          description: Nombre de usuario o identificador público en la plataforma externa
          example: "octocat"
        connected_at:
          type: string
          format: date-time
          nullable: true
          description: Marca de tiempo ISO-8601 UTC de la vinculación
          example: "2026-08-19T10:00:00Z"
      required: [provider, is_connected]

    # ── GitHub Sync (HU-23) ──

    GitHubSyncStatusEnum:
      type: string
      enum: [not_created, created, syncing, synced, failed]
      description: Estado del ciclo de vida y sincronización del repositorio remoto
      example: synced

    ProjectGitHubResponse:
      type: object
      description: Metadatos de vinculación y sincronización con GitHub para un proyecto
      properties:
        has_repository:
          type: boolean
          description: True si el proyecto ya cuenta con un repositorio remoto creado en GitHub
          example: true
        repo_name:
          type: string
          nullable: true
          description: Nombre del repositorio remoto en GitHub
          example: "kosmo-gestion-inventarios"
        repo_url:
          type: string
          nullable: true
          description: URL pública o accesible del repositorio en GitHub
          example: "https://github.com/octocat/kosmo-gestion-inventarios"
        is_public:
          type: boolean
          nullable: true
          description: Visibilidad del repositorio (True = público, False = privado)
          example: false
        last_push_at:
          type: string
          format: date-time
          nullable: true
          description: Marca de tiempo ISO-8601 UTC del último envío exitoso de código
          example: "2026-08-22T18:45:00Z"
        last_commit_hash:
          type: string
          nullable: true
          description: Hash SHA-1 del último commit sincronizado con el repositorio remoto
          example: "7f4b82d3e91a0c5b6e8f4a1c3d5e7b9a2f1e4c6a"
        sync_status:
          $ref: "#/components/schemas/GitHubSyncStatusEnum"
        error_message:
          type: string
          nullable: true
          description: Detalle del error en caso de fallo en la sincronización
          example: null
      required: [has_repository, sync_status]

    PushGitHubRequest:
      type: object
      additionalProperties: false
      description: Payload para solicitar la creación de repositorio o push incremental de código
      properties:
        repo_name:
          type: string
          minLength: 1
          maxLength: 100
          nullable: true
          description: Nombre deseado para el repositorio. Obligatorio si el repositorio aún no existe.
          example: "kosmo-gestion-inventarios"
        is_public:
          type: boolean
          default: false
          description: Visibilidad del repositorio (privado por defecto; público requiere confirmación previa)
          example: false
        commit_message:
          type: string
          maxLength: 300
          nullable: true
          description: Mensaje descriptivo para el commit de sincronización
          example: "feat: implementar autenticación y modelo de datos"
      required: []

    # ── Deploy Railway (HU-24) ──

    DeployStatusEnum:
      type: string
      enum: [idle, pending, building, ready, failed]
      description: Estado del ciclo de vida del despliegue en la plataforma en la nube
      example: ready

    ProjectDeployStatusResponse:
      type: object
      description: Metadatos y estado actual de la publicación del proyecto en la nube
      properties:
        service_id:
          type: string
          nullable: true
          description: Identificador único del servicio en Railway
          example: "srv_01HTXYZ9876543210ABC"
        service_name:
          type: string
          nullable: true
          description: Nombre del servicio desplegado en la plataforma
          example: "kosmo-gestion-inventarios"
        deploy_url:
          type: string
          nullable: true
          description: URL pública accesible en internet generada por Railway
          example: "https://kosmo-gestion-inventarios.up.railway.app"
        status:
          $ref: "#/components/schemas/DeployStatusEnum"
        last_deploy_at:
          type: string
          format: date-time
          nullable: true
          description: Marca de tiempo ISO-8601 UTC del último despliegue disparado
          example: "2026-08-22T19:00:00Z"
        error_message:
          type: string
          nullable: true
          description: Mensaje de error si la construcción o arranque del servicio fallaron
          example: null
        error_log_url:
          type: string
          nullable: true
          description: Enlace directo hacia la consola de registros de compilación en Railway
          example: "https://railway.com/project/prj-123/service/srv-456/logs"
      required: [status]

    DeployRailwayRequest:
      type: object
      additionalProperties: false
      description: Parámetros opcionales para configurar o personalizar el despliegue en Railway
      properties:
        service_name:
          type: string
          maxLength: 100
          nullable: true
          description: Nombre personalizado para el servicio en Railway
          example: "kosmo-gestion-inventarios"
        environment_variables:
          type: object
          additionalProperties:
            type: string
          nullable: true
          description: Variables de entorno adicionales a configurar en el servicio
          example:
            NODE_ENV: "production"
            LOG_LEVEL: "info"
      required: []
