import { Metadata } from 'next';

const metadata: Metadata = {
	title: 'Prototipo - KOSMO',
	description: '',
};

const PrototypePage = () => {
	return <div>PrototypePage</div>;
};

export { PrototypePage, metadata };
