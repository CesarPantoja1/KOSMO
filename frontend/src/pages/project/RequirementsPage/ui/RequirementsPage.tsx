import { Metadata } from 'next';

const metadata: Metadata = {
	title: 'Requisitos - KOSMO',
	description: '',
};

const RequirementsPage = () => {
	return <div>RequirementsPage</div>;
};

export { RequirementsPage, metadata };
